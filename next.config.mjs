/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['aceternity.com', 'images.unsplash.com', 'assets.aceternity.com', "api.microlink.io",],
  },};

export default nextConfig;
